<h1>Question:</h1>
<p><%= @question.body %></p>

<br><%= link_to 'Back', test_path(@question.test) %><br>
<td> <%= link_to 'Edit', edit_question_path(@question) %></td>
<br> <%= link_to 'Delete', question_path(@question), method: :delete, data: {confirm: "Are you sure?"} %><br>
