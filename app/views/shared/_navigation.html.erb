<nav class='navbar navbar-light bg-light nav user'>

  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
    <span class="navbar-text">
      <% if user_signed_in? %>
        Welcome, <b class='user-email'><%= current_user.email %></b> Guru | <%= link_to 'Log out', destroy_user_session_path, method: :delete%>
        <%= link_to t('shared.action.badges'), badges_path %> |

        <%if current_user_admin? %> |
        <%= link_to t('navigation.admin_panel'), admins_tests_path%>
          <% end %>
      <% else %>
          <%= link_to t('navigation.log_in'), new_session_path(resource_name) %> |
          <%= link_to t('navigation.sign_up'), new_registration_path(resource_name) %>
      <% end %>

  </span>
</nav>
