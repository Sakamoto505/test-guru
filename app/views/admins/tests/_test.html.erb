<div class="row justify-content-center mt-1">
  <div class="col">

<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.category.name %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('shared.action.show'), admin_test_path(test), class: "btn btn-primary btn-circle" %></td>
  <td><%= link_to t('shared.action.edit'), edit_admin_test_path(test), class: "btn btn-primary btn-circle"  %></td>

  <td> <%= link_to t('shared.action.delete'),
                   admin_test_path(test),class: "btn btn-primary btn-circle",
           method: :delete,
                   data: { confirm: t('shared.action.confirm')} %> </td>
</tr>
  </div>
  </div>
