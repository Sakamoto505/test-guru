<div class="container">
  <div class="row">
    <div class="col">
      <h1><%= t('gists.index.gists') %></h1>
      <table class="table">
        <tr>
          <th><%= t('gists.index.question') %></th>
          <th><%= t('gists.index.links') %></th>
          <th><%= t('gists.index.user') %></th>
          <%= render @gists %>
        <tr>
      </table>
    </div>
  </div>
<%= link_to t('gists.index.back'), admin_tests_path %>
</div>
